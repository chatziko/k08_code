#pragma once // modern include guard

// This file is part of demo3
//
// demo3 uses stacks, so we need to set the StItem properly.
// For our purposes we need a stack of ints, so...

typedef int StItem;
